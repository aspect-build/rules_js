load("//js:defs.bzl", "js_test")

# Verifies that the directory the launcher puts on the PATH holds exactly one
# PATH-resolvable node binary.
js_test(
    name = "node_bin_path_test",
    entry_point = "node_bin_path_test.js",
)

js_test(
    name = "node_bin_path_include_npm_test",
    entry_point = "node_bin_path_test.js",
    include_npm = True,
)
